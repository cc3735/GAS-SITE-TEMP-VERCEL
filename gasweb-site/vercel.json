{
  "installCommand": "npm install && cd ../business-apps/LegalFlow && npm install --production && cd ../FinanceFlow && npm install --production",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
